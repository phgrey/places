<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>CoworkersMy</title>
  <%= csrf_meta_tags %>

  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->

  <!-- Le styles -->
  <%= stylesheet_link_tag "application", :media => "all" %>
  <style>
      body {
          padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
  </style>

  <!-- Le fav and touch icons -->
  <link href="images/favicon.ico" rel="shortcut icon">
  <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
  <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
  <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
</head>
<body>

<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="i-bar"></span>
        <span class="i-bar"></span>
        <span class="i-bar"></span>
      </a>
      <a class="brand" href="/">CoworkersMy</a>

      <div class="container nav-collapse">
        <ul class="nav pull-right">
          <li><%= link_to "Offers", offers_path %></li>
          <li><%= link_to "Users", users_path %></li>
          <li><%= link_to "Link3", "/path3" %></li>
          <li class="divider-vertical"></li>
          <li class="dropdown">
            <a style="cursor: pointer;" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-user icon-white"></i> <%= user_signed_in? ? current_user.name : 'User' %>
              <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <% if (user_signed_in?) %>
                  <li><a href="<%= destroy_user_session_path %>"><i class="icon-off"></i> Logout</a></li>
                  <li><a href="<%= edit_user_registration_path %>"><i class="icon-pencil"></i> Profile</a></li>
              <% else %>
                  <li><a href="<%= new_user_session_path %>"><i class="icon-user"></i> Login</a></li>
                  <li><a href="<%= new_user_registration_path %>"><i class="icon-plus"></i> Register</a></li>
              <% end %>
              <li class="divider"></li>
              <li><a href="/"><i class="icon-home"></i> Main page</a></li>
            </ul>
          </li>
        </ul>
      </div>
      <!--/.nav-collapse -->
    </div>
  </div>
</div>

<div class="container">
  <div class="content">
    <div class="row">
      <div class="span9">
        <%== render_crumbs %>
        <% if (notice) %>
            <div class="alert alert-success"><%= notice %></div>
        <% end %>
        <% if (alert) %>
            <div class="alert alert-error"><%= alert %></div>
        <% end %>
        <%= yield %>
      </div>
      <div class="span1">&nbsp;</div>
      <div class="span3">
        <div class="well sidebar-nav">
          <h3>Sidebar</h3>
          <ul class="nav nav-list">
            <li class="nav-header">Sidebar</li>
            <li><%= link_to "Link1", "/path1" %></li>
            <li><%= link_to "Link2", "/path2" %></li>
            <li><%= link_to "Link3", "/path3" %></li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <footer>
    <p>&copy; Company 2012</p>
  </footer>

</div>
<!-- /container -->

<!-- Le javascript
================================================== -->
<!-- Placed at the end of the document so the pages load faster -->
<%= javascript_include_tag "application" %>

</body>
</html>
